## Packages
framer-motion | Smooth animations for page transitions and scroll reveals
react-intersection-observer | Scroll-triggered animations

## Notes
- This is a static frontend-only build as requested.
- Contact form uses mock submission (no backend endpoint required).
- Images use Unsplash source URLs.
- Google Fonts: Playfair Display (Headings) + Inter (Body).
